---
derived_from:
  - constitution.md
  - spec.md
  - [module spec]
  - [trace slice]
# change_class: local | behavioral-in-module | boundary-changing
#   local — changes confined to internal implementation; no interface or contract changes
#   behavioral-in-module — changes to behavior within one module; may touch module spec
#   boundary-changing — changes that cross module boundaries or modify interface contracts
change_class: local | behavioral-in-module | boundary-changing
scope_owner: [owner]
---

# Plan: [Change Name]

<!-- A plan is derived execution guidance — it is NOT a canonical semantic
     contract. It is generated by the Agent when a change is requested and
     describes the tasks needed to implement that change.

     The plan traces every task back to governing requirements/stories and
     forward to acceptance signals (tests). It is regenerated if upstream
     artifacts change.

     Include in both Lite and Full profiles. -->

This file is derived execution guidance. It is not a canonical semantic contract.

## Change Summary

<!-- Concise description of what is being changed and why.
     Reference governing IDs from upstream artifacts. -->

- Requested change: <!-- Example: "Add email verification to registration flow" -->
- Governing requirement/story IDs: <!-- Example: PRD-FR-001, STORY-001 -->
- Touched entities / invariants: <!-- Example: ENT-001 (User), INV-001 (email uniqueness) -->
- Touched modules / interfaces: <!-- Example: MOD-auth, IFACE-001 (Full profile) -->

## Context Slice

<!-- Artifacts the Agent must load to execute this plan.
     Keep this minimal — only load what the change actually touches. -->

| Artifact | Reason |
| --- | --- |
| `constitution.md` | Universal rules and ID grammar |
| `spec.md` | Technical boundaries and API design |
| `dm.md` | Entity invariants for ENT-001 |
| `usm.md` | Acceptance criteria for STORY-001 |
| `modules/mod-auth/spec.md` | Module interface contracts (Full profile) |

## Task Graph

<!-- Ordered list of implementation tasks. Each task:
     - Has a stable TASK-xxx ID
     - Declares its dependencies (other TASK IDs)
     - Maps to a validation signal (TEST-xxx)
     Tasks without dependencies can be executed in parallel. -->

| ID | Task | Depends on | Validation |
| --- | --- | --- | --- |
| TASK-001 | Add `verified` field to User entity | — | TEST-001 |
| TASK-002 | Create email verification endpoint | TASK-001 | TEST-002 |
| TASK-003 | Send verification email on registration | TASK-001 | TEST-003 |
| TASK-004 | Update login to check verification status | TASK-002 | TEST-004 |
| TASK-005 | | | TEST-xxx |

## Acceptance Signals

<!-- How each task is verified. Source traces to the story or requirement
     that this test validates. -->

| ID | Signal | Source |
| --- | --- | --- |
| TEST-001 | User table migration runs; `verified` column exists with default `false` | STORY-001 |
| TEST-002 | GET /api/auth/verify?token=valid returns 200 and sets verified=true | STORY-001, AC-001 |
| TEST-003 | Registration triggers email send with valid verification link | STORY-001, AC-002 |
| TEST-004 | Unverified user login returns 403 with "please verify email" message | STORY-001, AC-003 |
| TEST-005 | | STORY-xxx |

## Reconcile Notes

<!-- Track upstream and downstream ripple effects of this change.
     Stale edges: which downstream artifacts become stale after this change. -->

- Upstream changes required: <!-- Example: "None — PRD-FR-001 already covers verification" -->
- Downstream changes required: <!-- Example: "Update AGENTS.md to include new endpoint" -->
- Stale edges triggered: <!-- Example: "agents.md becomes stale; module-spec needs IFACE update" -->
